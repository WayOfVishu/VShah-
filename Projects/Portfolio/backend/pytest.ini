[pytest]
# Tests import `app.*`, so the backend directory has to be on sys.path.
pythonpath = .
testpaths = tests
filterwarnings =
    # starlette's TestClient warns that httpx is deprecated in favour of httpx2.
    # Not worth churning the dev dependency over; revisit when httpx2 is stable.
    ignore::DeprecationWarning
